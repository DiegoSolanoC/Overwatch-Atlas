/**
 * Draw order for event markers, pins, and pulse rings relative to clouds / aurora / rim.
 * Pulse ring order must stay aligned with {@link MarkerPulseService} (classic script; duplicates value).
 */
export const EVENT_PIN_RENDER_ORDER = 14;
export const EVENT_MARKER_RENDER_ORDER = 15;
export const EVENT_PULSE_RING_RENDER_ORDER = 17;
