/**
 * Gallery personal connection canvas — tuning separate from main Codex.
 */

/** Max node portrait scale in the gallery personal canvas (main Codex caps at 5). */
export const GALLERY_CONN_SCALE_MAX = 10;
